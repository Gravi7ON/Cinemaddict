const Films = {
  RATED_LIST: 'rated',
  COMMENTED_LIST: 'commented'
};

const FILMS_COUNT_PER_STEP = 5;

const Mode = {
  DEFAULT: 'DEFAULT',
  POPUP: 'POPUP',
};

const SortType = {
  DEFAULT: 'default',
  DATE: 'date',
  RATING: 'rating',
};

export {Films, Mode, SortType, FILMS_COUNT_PER_STEP};
